# Database connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=url_shortener
DB_USERNAME=shortener
DB_PASSWORD=change-me

# JWT configuration
# JWT_SECRET must be at least 32 characters (256 bits) for the HS256 algorithm
JWT_SECRET=please-change-this-to-a-long-random-secret-value
JWT_EXPIRATION_MS=86400000

# Application
SERVER_PORT=8080
APP_BASE_URL=http://localhost:8080
SHORT_LINK_TTL_DAYS=30
